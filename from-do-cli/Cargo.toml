[package]
name = "from-do-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
notify = "8.2.0"
notify-debouncer-mini = "0.7.0"
from-do-compiler = { path = "../from-do-compiler" }
